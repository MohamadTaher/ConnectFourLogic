public abstract class Player {

    // those two abstract methods that will be given body inside the child class
    abstract char getPlayerType();

    abstract int makePlayerMove(BoardObject board); 
}
